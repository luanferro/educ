import 'package:flutter_test/flutter_test.dart';

maain() {
  // NotaRepository repository = NotaRepositoryImp();

  test('Devolva uma nota', () {
    //var result = repository.getNotas(nomeAluno);

    //expect(result, isNotNull);
  });
}
